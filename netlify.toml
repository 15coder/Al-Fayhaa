[build]
  command = "pnpm install && pnpm --filter @workspace/al-fayhaa-institute build"
  publish = "artifacts/al-fayhaa-institute/dist/public"

[build.environment]
  NODE_ENV = "production"
  BASE_PATH = "/"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
